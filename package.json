{
  "name": "yundict",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist",
    "build:declaration": "tsc --emitDeclarationOnly",
    "test": "bun test"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.14"
  }
}